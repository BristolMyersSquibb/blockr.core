% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-pkg.R
\name{otel_lbl}
\alias{otel_lbl}
\title{Build an OTel-friendly observer label}
\usage{
otel_lbl(name, env = parent.frame())
}
\arguments{
\item{name}{Short, snake_case identifier describing the observer.}

\item{env}{An environment used to resolve the package name; defaults to
the caller's frame.}
}
\value{
A string of the form \code{"<pkg>name"}.
}
\description{
Returns \code{name} prefixed with the calling package's name in angle brackets,
so that observer labels emitted into OpenTelemetry traces are unambiguous
across \code{blockr.core}, \code{blockr.dock}, and downstream extension packages.
For example, calling \code{otel_lbl("add_block")} from inside \code{blockr.dock}
returns \code{"<blockr.dock>add_block"}.
}
\details{
Intended for use as the \code{label} argument of \code{\link[shiny:observeEvent]{shiny::observeEvent()}},
\code{\link[shiny:observe]{shiny::observe()}}, \code{\link[shiny:reactive]{shiny::reactive()}}, and \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}}.
The package name is resolved at call time via \code{\link[=pkg_name]{pkg_name()}} applied to
\code{env}, which defaults to the immediate caller's environment.
}
\examples{
# Inside a package, equivalent to label = "<your.pkg>my_obs"
otel_lbl("my_obs")

}
