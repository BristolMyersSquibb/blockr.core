% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block-args.R
\name{arg-type}
\alias{arg-type}
\alias{arg_string}
\alias{arg_number}
\alias{arg_integer}
\alias{arg_boolean}
\alias{arg_enum}
\alias{arg_array}
\alias{arg_object}
\title{Argument type descriptors}
\usage{
arg_string(description = NULL, required = TRUE)

arg_number(description = NULL, required = TRUE)

arg_integer(description = NULL, required = TRUE)

arg_boolean(description = NULL, required = TRUE)

arg_enum(values, description = NULL, required = TRUE)

arg_array(items, description = NULL, required = TRUE)

arg_object(..., description = NULL, required = TRUE)
}
\arguments{
\item{description}{Human- and model-facing description of the value}

\item{required}{Whether the field is required, when nested in an
\code{arg_object()}}

\item{values}{Allowed string values, for \code{arg_enum()}}

\item{items}{Element descriptor, for \code{arg_array()}}

\item{...}{Named field descriptors, for \code{arg_object()}}
}
\value{
A plain list describing the argument value as a JSON-Schema node.
}
\description{
The optional \code{type} of a block argument (see \code{\link[=new_block_arg]{new_block_arg()}}) is described
with a small, dependency-free subset of JSON Schema, built with these
constructors. Each returns a plain nested list mirroring the schema it
denotes: \code{arg_string()} / \code{arg_number()} / \code{arg_integer()} / \code{arg_boolean()}
for scalars, \code{arg_enum()} for a fixed set of string values, \code{arg_array()} for
a homogeneous list and \code{arg_object()} for a closed record of named fields
(\code{additionalProperties: false}). The descriptor is consumed directly --
blockr.ai binds it via \code{ellmer::type_from_schema()}, an MCP or raw tool
schema reads the JSON as-is -- and worked examples registered alongside an
argument are validated against it (see \code{\link[=register_block]{register_block()}}).
}
\details{
Semantic intent (e.g. "a column in the upstream data", "an R expression") is
carried in \code{description}, not in the type vocabulary.
}
\examples{
arg_object(
  conditions = arg_array(
    arg_object(column = arg_string(), value = arg_string())
  ),
  operator = arg_enum(c("&", "|"))
)

}
