% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocks-class.R, R/links-class.R,
%   R/stacks-class.R
\name{split.blocks}
\alias{split.blocks}
\alias{split.links}
\alias{split.stacks}
\title{Split container objects into singletons}
\usage{
\method{split}{blocks}(x, f = seq_along(x), ...)

\method{split}{links}(x, f = seq_along(x), ...)

\method{split}{stacks}(x, f = seq_along(x), ...)
}
\arguments{
\item{x}{A container object (\code{blocks}, \code{links} or \code{stacks}).}

\item{f}{Grouping passed to \code{\link[base:split]{base::split()}}. Defaults to \code{seq_along(x)}, i.e.
one group per element with order preserved.}

\item{...}{Forwarded to \code{\link[base:split]{base::split()}}.}
}
\value{
A list of container objects, each of the same class as \code{x}, one per
level of \code{f}. With the default \code{f}, a list of one-element containers in
input order.
}
\description{
\code{\link[base:split]{base::split()}} methods for the container classes \link{blocks}, \link{links} and
\link{stacks}. The only departure from the base generic is that the grouping \code{f}
defaults to \code{seq_along(x)}, so \code{split(x)} returns each element wrapped in a
one-element container of the same class, in input order. This is the
discoverable idiom for iterating a container as singleton containers (e.g.
\code{lapply(split(x), f)}) rather than as bare elements (\code{lapply(x, f)}) or via
the verbose \code{split(x, seq_along(x))}. Passing an explicit \code{f} recovers
standard \code{\link[base:split]{base::split()}} grouping semantics.
}
\examples{
blks <- c(a = new_dataset_block(), b = new_subset_block())
split(blks)

}
