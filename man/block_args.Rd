% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block-args.R
\name{block_arg}
\alias{block_arg}
\alias{block_args}
\alias{block_arg_specs}
\alias{block_examples}
\title{Block argument specification}
\usage{
block_arg(description = NULL, example = NULL, type = NULL)

block_args(...)

block_arg_specs(id)

block_examples(id)
}
\arguments{
\item{description}{Human- and model-facing description of the argument}

\item{example}{A single worked value for the argument (or \code{NULL})}

\item{type}{Optional machine-readable type (e.g. an \verb{ellmer::type_*}),
stored opaquely and not interpreted by blockr.core}

\item{...}{Named \code{block_arg()} objects (or strings, taken as descriptions),
one per constructor formal}

\item{id}{Registry ID as reported by \code{\link[=list_blocks]{list_blocks()}}}
}
\value{
\code{block_arg()} returns a \code{block_arg} object and \code{block_args()} a
\code{block_args} object. \code{block_arg_specs()} returns the \code{block_args} object of
a registered block, while \code{block_examples()} returns a list of complete
worked configurations (each a named list keyed by argument name).
}
\description{
Block constructor arguments can be documented in the registry with a
structured specification instead of a bare named character vector of
descriptions. Each argument is described via \code{block_arg()} (a human/model
\code{description}, an optional worked \code{example} value, and an optional
machine-readable \code{type}), and the per-argument specs are collected with
\code{block_args()}. A bare named character vector (descriptions only) and the
empty \code{character()} remain valid and are normalized into this representation,
so existing registrations are unaffected.
}
\details{
The worked \code{example} of an argument is a single value. A complete worked
configuration of a block is the assembly of its per-argument examples, keyed
by argument name -- this is what \code{block_examples()} returns and what model
consumers render. When arguments interact, or several few-shot examples are
wanted, complete configurations are instead supplied as a list via the
\code{examples} argument of \code{\link[=register_block]{register_block()}}; these supersede the per-argument
assembly. Multiple per-argument examples are intentionally not supported:
there is no safe way to combine them into coherent whole-block
configurations.
}
\examples{
spec <- block_args(
  n = block_arg(description = "Number of rows", example = 5L)
)

}
