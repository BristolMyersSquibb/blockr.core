% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabular-display.R
\name{tabular-display}
\alias{tabular-display}
\alias{new_tabular_display}
\alias{minimal_display}
\alias{dt_display}
\alias{is_tabular_display}
\alias{tabular_display}
\alias{tabular_ui}
\alias{tabular_output}
\alias{tabular_render_trigger}
\alias{tabular_board_options}
\title{Tabular result display}
\usage{
new_tabular_display(subclass)

minimal_display

dt_display

is_tabular_display(x)

tabular_display()

tabular_ui(x, id)

tabular_output(x, result, block, session)

tabular_render_trigger(x, session)

tabular_board_options(x, ...)
}
\arguments{
\item{subclass}{Display sub-class string}

\item{x}{Tabular display object}

\item{id}{Namespace ID for the output container}

\item{result}{Block result}

\item{block}{Block object (used to read board option defaults)}

\item{session}{Shiny session object}

\item{...}{Forwarded to board option constructors}
}
\value{
\code{new_tabular_display()} and \code{tabular_display()} return a
\code{tabular_display} object and \code{is_tabular_display()} a boolean.
\code{tabular_ui()} returns shiny UI, \code{tabular_output()} the result of a shiny
render function, \code{tabular_render_trigger()} is called for its reactive side
effect (invisibly returning the option values it depends on) and
\code{tabular_board_options()} a \link[=new_board_options]{board_options} set.
}
\description{
How a data, parser or transform block previews its result is governed by a
\emph{tabular display}: an S3 object selecting a coherent set of methods for the
output container (\code{tabular_ui()}), the render function (\code{tabular_output()}),
the render trigger (\code{tabular_render_trigger()}) and the board options the
preview relies on (\code{tabular_board_options()}). Bundling all four on one
object keeps them in sync: a display cannot declare a \code{page_size} option it
then fails to honor, or render through DT while its container is plain text.
}
\details{
The active display is resolved from the \code{blockr.tabular_display} option (via
\code{\link[=blockr_option]{blockr_option()}}) and defaults to \code{minimal_display}, a dependency-free
preview of the top rows. \code{dt_display} reinstates the paginated, searchable
DT table. Downstream packages provide further displays by defining methods
for the four generics on their own \code{tabular_display} sub-class and having
users opt in with \verb{options(blockr.tabular_display = <display>)}.
}
