% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-shiny.R
\name{trim_rv}
\alias{trim_rv}
\title{Remove entries from a \code{reactiveValues} object}
\usage{
trim_rv(x, rm)
}
\arguments{
\item{x}{A \code{reactiveValues} object.}

\item{rm}{Character vector of keys to remove; all must be present in \code{x}.}
}
\value{
\code{x}, invisibly.
}
\description{
shiny offers no public way to delete a key from a \code{\link[shiny:reactiveValues]{shiny::reactiveValues()}}
object -- assigning \code{NULL} stores a \code{NULL} value but leaves the key in
\code{names()}. \code{trim_rv()} removes the named entries outright and invalidates
the affected reactive dependencies, so a key can be truly dropped (and
later re-added) -- for instance when a variadic block argument is unlinked.
}
