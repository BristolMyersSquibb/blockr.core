% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/board-server.R
\name{validate_board_update}
\alias{validate_board_update}
\title{Validate a board update payload}
\usage{
validate_board_update(payload, board)
}
\arguments{
\item{payload}{A list of the shape accepted by the \code{board_update}
reactiveVal, with optional \code{blocks}, \code{links}, and \code{stacks} slots, each
a list with optional \code{add}, \code{mod}, and \code{rm} entries.}

\item{board}{A \code{board} object to validate the payload against.}
}
\value{
\code{invisible(payload)} on success. On failure, throws a
\code{blockr_abort()} error with a class such as \verb{board_update_*_invalid}.
}
\description{
Run the same validation logic that the \code{board_server()} observer applies
to every \code{board_update} payload, but against a caller-supplied payload
value and board. Useful for downstream packages that want to surface
validation errors before committing a proposed update — for example a
staging layer that accumulates pending changes from LLM tool calls and
needs each call to fail loudly if it would conflict with the live board.
}
\details{
Validation runs in two passes: a structural check on the payload shape
and per-slot block / link / stack rules, followed by a cross-reference
check that link \code{from}/\code{to} endpoints and stack members resolve in the
post-update merged view.

Preprocessing (auto-cleanup of dangling refs from block removals) is an
apply-time concern and is \strong{not} performed here — the payload is
validated as-is.
}
\examples{
brd <- new_board(
  blocks = c(a = new_dataset_block("iris"), b = new_subset_block()),
  links = links(ab = new_link(from = "a", to = "b"))
)

validate_board_update(
  list(links = list(rm = "ab")),
  brd
)

try(
  validate_board_update(
    list(links = list(add = links(xy = new_link(from = "x", to = "y")))),
    brd
  )
)

}
